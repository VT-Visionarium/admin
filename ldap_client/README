Setup the login authentication to use ldap://login.directory.vt.edu


Edit /etc/ssh/sshd_config and make sure it has the line:
UsePAM yes



maybe run:


  sudo cp -a /etc/ /root/etc_BACKUP


run:

  sudo ./root_install_1.bash

and

  sudo ./root_install_2.bash
